# shared/lzstring-vectors

LZ-string test vectors: compressed `mb_recovery` input → expected recovery payload.

Both SDKs implement a native port of LZ-string `decompressFromEncodedURIComponent`
and must decode these vectors identically. See SPEC.md §11 and §14.
Populated in later slices.
