# shared/push-samples

Push payload samples: raw push payload → expected `FlowbizPush` result.

Both SDKs must parse these samples identically in `handlePush`.
See SPEC.md §10 and §14. Populated in later slices.
