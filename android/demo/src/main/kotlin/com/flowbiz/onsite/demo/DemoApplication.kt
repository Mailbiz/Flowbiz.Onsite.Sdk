package com.flowbiz.onsite.demo

import android.app.Application

/**
 * Minimal application stub. The fake-store demo screens (SPEC §14) are built
 * in later slices.
 */
class DemoApplication : Application()
